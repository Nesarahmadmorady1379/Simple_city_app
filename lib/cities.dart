class City {
  String? name;
  String? capital;
  int? numberOfDistricts;
  int? population;
  String? area;
  String? url;

  City(this.name, this.capital, this.numberOfDistricts, this.population,
      this.area, this.url);
}